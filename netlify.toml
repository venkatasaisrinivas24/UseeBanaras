[build]
  command = "npm run build"
  publish = "dist"
  functions = "dist/server"

[context.production]
  command = "npm run build"
  publish = "dist"

[context.deploy-preview]
  command = "npm run build:dev"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
